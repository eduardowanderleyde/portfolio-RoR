<div class="min-h-screen bg-gradient-to-br from-blue-600 to-indigo-700 relative overflow-hidden">
  <!-- Navbar -->
  <nav class="container mx-auto px-6 py-6">
    <div class="flex justify-between items-center">
      <div class="text-white text-2xl font-bold">DevMan</div>
      <div class="hidden md:flex items-center space-x-8">
        <%= link_to "Home", root_path, class: "text-white/90 hover:text-white" %>
        <%= link_to "About Me", about_path, class: "text-white/90 hover:text-white" %>
        <%= link_to "Portfolio", projects_path, class: "text-white/90 hover:text-white" %>
        <%= link_to "Contact", contact_path, class: "text-white/90 hover:text-white" %>
        <%= link_to "Download CV", "#", class: "bg-red-500 text-white px-6 py-2 rounded-full hover:bg-red-600 transition-all" %>
      </div>
    </div>
  </nav>

  <!-- Header -->
  <div class="bg-gray-900 text-white py-20">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h1 class="text-4xl font-bold mb-4">Sobre Mim</h1>
      <p class="text-xl text-gray-400">Desenvolvedor apaixonado por criar soluções inovadoras</p>
    </div>
  </div>

  <!-- Main Content -->
  <main class="container mx-auto px-6 py-20">
    <!-- About Section -->
    <div class="max-w-4xl mx-auto">
      <div class="text-center mb-16">
        <h1 class="text-4xl md:text-5xl font-bold text-white mb-6">Sobre Mim</h1>
        <p class="text-xl text-white/80">Desenvolvedor apaixonado por criar soluções elegantes para problemas complexos</p>
      </div>

      <!-- Profile Section -->
      <div class="bg-white/10 backdrop-blur-lg rounded-3xl p-8 mb-16">
        <div class="flex flex-col md:flex-row gap-12 items-center">
          <div class="md:w-1/3">
            <%= image_tag "profile.jpeg", class: "rounded-2xl w-full aspect-square object-cover shadow-xl", style: "filter: contrast(1.1) brightness(1.1);" %>
          </div>
          <div class="md:w-2/3 text-white">
            <h2 class="text-3xl font-bold mb-6">Eduardo Wanderley</h2>
            <p class="text-white/80 mb-6">
              Sou um desenvolvedor Full Stack com mais de 5 anos de experiência, especializado em Ruby on Rails.
              Minha paixão é criar aplicações web que não apenas funcionem perfeitamente, mas também proporcionem
              uma experiência excepcional aos usuários.
            </p>
            <div class="grid grid-cols-2 gap-6">
              <div>
                <h3 class="font-semibold mb-2">Localização</h3>
                <p class="text-white/80">Brasil</p>
              </div>
              <div>
                <h3 class="font-semibold mb-2">Experiência</h3>
                <p class="text-white/80">5+ Anos</p>
              </div>
              <div>
                <h3 class="font-semibold mb-2">Disponível para</h3>
                <p class="text-white/80">Projetos Freelance</p>
              </div>
              <div>
                <h3 class="font-semibold mb-2">Idiomas</h3>
                <p class="text-white/80">Português, Inglês</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Skills Section -->
      <div class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-8">Minhas Habilidades</h2>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
          <% @skills.each do |skill| %>
            <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 text-center">
              <div class="text-3xl mb-4">
                <%= case skill
                    when 'Ruby on Rails' then '💎'
                    when 'PostgreSQL' then '🗄️'
                    when 'JavaScript' then '⚡'
                    when 'HTML/CSS' then '🎨'
                    when 'Git' then '📦'
                    when 'Docker' then '🐳'
                    when 'AWS' then '☁️'
                    when 'TDD' then '🧪'
                    else '💻'
                    end %>
              </div>
              <h3 class="text-white font-semibold"><%= skill %></h3>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Experience Section -->
      <div class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-8">Experiência Profissional</h2>
        <div class="space-y-6">
          <% @experiences.each do |experience| %>
            <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6">
              <div class="flex flex-col md:flex-row justify-between md:items-center mb-4">
                <div>
                  <h3 class="text-xl font-bold text-white"><%= experience.role %></h3>
                  <p class="text-white/80"><%= experience.company %></p>
                </div>
                <div class="text-white/70 text-sm">
                  <%= experience.start_date.strftime("%B %Y") %> - 
                  <%= experience.end_date ? experience.end_date.strftime("%B %Y") : "Presente" %>
                </div>
              </div>
              <p class="text-white/80"><%= experience.description %></p>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Education Section -->
      <div>
        <h2 class="text-3xl font-bold text-white mb-8">Educação</h2>
        <div class="space-y-6">
          <% @educations.each do |education| %>
            <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6">
              <div class="flex flex-col md:flex-row justify-between md:items-center mb-4">
                <div>
                  <h3 class="text-xl font-bold text-white"><%= education.degree %></h3>
                  <p class="text-white/80"><%= education.institution %></p>
                </div>
                <div class="text-white/70 text-sm">
                  <%= education.start_date.strftime("%B %Y") %> - 
                  <%= education.end_date ? education.end_date.strftime("%B %Y") : "Presente" %>
                </div>
              </div>
              <p class="text-white/80"><%= education.description %></p>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </main>

  <!-- Background Blur Elements -->
  <div class="absolute top-0 right-0 w-1/2 h-1/2 bg-blue-500/30 rounded-full filter blur-[120px]"></div>
  <div class="absolute bottom-0 left-0 w-1/2 h-1/2 bg-indigo-500/30 rounded-full filter blur-[120px]"></div>
</div> 